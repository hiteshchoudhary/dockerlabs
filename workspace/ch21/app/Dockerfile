# Scaffolded for Chapter 21 — nothing here changes between environments.
# That's the point: config lives OUTSIDE the image.
FROM node:22-alpine
WORKDIR /usr/src/app
COPY server.js ./
EXPOSE 3000
CMD ["node", "server.js"]
